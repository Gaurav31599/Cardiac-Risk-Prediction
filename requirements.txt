# Core pipeline
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Primary gradient-boosting model (falls back to sklearn HistGBM if absent)
xgboost>=2.0

# Full algorithm sweep + experiment tracking
lightgbm>=4.0
mlflow>=2.9

# Explainability
shap>=0.44
matplotlib>=3.7

# Optional Streamlit demo
streamlit>=1.30

# Optional REST API for the HTML frontend (api.py)
fastapi>=0.110
uvicorn>=0.27
joblib>=1.3

# Dev / quality
pytest>=7.0
ruff>=0.4
